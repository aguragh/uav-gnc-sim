fastapi
uvicorn[standard]
numpy

# matplotlib is not listed here -- python/validate.py needs it locally,
# but the deployed web app never imports it. Install separately for validate.py:
#   pip install matplotlib
